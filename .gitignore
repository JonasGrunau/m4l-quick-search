# dependencies
node_modules/
node/node_modules/

# build output (regenerated by `npm run build`) — the device, brain, UI bundle
# and patcher all live here. Releases ship a zip of dist/ + html/ (see README).
dist/

# os / editor
.DS_Store
*.log

# local tooling state (oh-my-claudecode plugin)
.omc/
