# dependencies
node_modules/
node/node_modules/

# build output (regenerated by `npm run build`)
dist/
QuickSearch.maxpat
QuickSearch Dev.amxd

# the frozen release device is committed deliberately when shipping a version
# (see README) — it is NOT ignored: QuickSearch.amxd

# os / editor
.DS_Store
*.log

# local tooling state (oh-my-claudecode plugin)
.omc/
