# dependencies
node_modules/
node/node_modules/

# build output (regenerated by `npm run build`) — the device, brain, UI bundle,
# patcher, and the staged node.script + remote-script files all live here.
# Releases ship a zip of dist/ + remote-script/ (see README).
dist/

# python bytecode (Live compiles the Remote Script on load)
__pycache__/

# os / editor
.DS_Store
*.log

# local tooling state (oh-my-claudecode plugin)
.omc/
