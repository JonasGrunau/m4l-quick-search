<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-06-20 | Updated: 2026-06-20 -->

# docs

## Purpose
Design and reference documentation for M4L QuickSearch. This directory holds the canonical implementation plan that drove the architecture and all locked decisions (four-layer bridge design, Python Remote Script constraint, jweb injection strategy, design tokens), plus the binary screenshot asset used in the root README hero.

## Key Files

| File | Description |
|---|---|
| PLAN.md | Full design and implementation plan: architecture diagram, repo layout, 10 build milestones with completion status, design system tokens (colors, motion, typography), verification checklists, and top risks with mitigations. Includes a critical correction note (2026-06-20) documenting that Live's browser is NOT in the Max LiveAPI — only in the Python Remote Script API — which forced the Node bridge / Remote Script re-architecture. |
| overlay.png | Binary PNG — the Spotlight-card-over-Live image used as the hero in the root README (referenced via a relative HTML `<img src="docs/overlay.png" …>` tag, not a Markdown image). (Re)generated by `tools/screenshot.mjs`: Playwright renders the inlined UI bundle in design-preview mode and clips the rounded card on a transparent canvas. |

## For AI Agents

### Working In This Directory
- PLAN.md is the authoritative source of truth for architectural decisions and constraints. When the brief and the code diverge, check PLAN.md's "Correction" block first — it documents the LiveAPI browser false premise that was only discovered in a real Ableton run.
- The "Correction (2026-06-20)" note at the top of PLAN.md supersedes all passages in the same file that describe a `new LiveAPI(null, "live_app browser")` walk. Those stale passages are left in place with explicit "WRONG / superseded" labels.
- Milestone status markers (`✓` done, `◷` in progress, `⚠️` gate) in PLAN.md track real implementation state — do not assume all milestones are complete (Milestone 10 / freeze/distribute is still `◷`).
- overlay.png is generated by `tools/screenshot.mjs` (Playwright renders the inlined UI bundle in design-preview mode — sample devices, no Max bridge — and clips the rounded card on a transparent canvas). Regenerate it through that script (`npm i --no-save playwright && npx playwright install chromium`, then `node tools/screenshot.mjs`) rather than editing the PNG by hand.
- There are no subdirectories here; PLAN.md is the only prose document and it is deliberately kept as a single file.

### Testing / Verifying Changes
- PLAN.md has no automated tests — changes are validated by reading and cross-checking against the actual source files in `src/`, `node/`, `remote-script/`, and `html/`.
- If you update the architecture section or repo layout table in PLAN.md, verify it still matches the real directory tree (`ls -R` from the repo root) and the actual inlet/outlet contract at the top of `src/quicksearch.ts`.
- overlay.png is rendered from the inlined UI bundle by `tools/screenshot.mjs`; after a UI change, regenerate it (`node tools/screenshot.mjs`, Playwright required) so the README hero matches the current overlay. `npm run dev` previews the same UI in the browser for a quick visual check.

### Common Patterns
- PLAN.md uses a "Load-bearing facts" subsection with strikethrough markup (`~~…~~`) + bold "WRONG / superseded" labels to preserve historical context when a fact is invalidated, rather than deleting the original claim. Follow this pattern when adding further corrections.
- Design tokens are documented as a Markdown table in PLAN.md (`## Design system`) — these values are the single source of truth for `html/styles.css`. When CSS tokens change, update the table here.
- Risk mitigations in PLAN.md (`## Top risks`) are numbered and paired; each risk entry calls out both the failure mode and the concrete mitigation baked into the architecture.

## Dependencies

### Internal
- Referenced by the root `README.md` (overlay.png via relative image link).
- PLAN.md describes and depends on `src/`, `html/`, `node/`, `remote-script/`, and `tools/` — changes to any of those layers may require updating the architecture diagram, repo layout, or milestone status in PLAN.md.

### External
- No external packages or build tools act on this directory at runtime or build time. PLAN.md mentions `amxd_textconv.py` (Ableton's own parser, used by `npm run build` for round-trip validation) and `uiohook-napi` (opt-in global hotkey in `node/`) as external dependencies of the broader project, but neither processes files in `docs/`.

<!-- MANUAL: Notes added below this line are preserved on regeneration -->
