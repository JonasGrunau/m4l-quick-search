# CLAUDE.md

This project's AI-agent documentation lives in hierarchical `AGENTS.md` files —
one per directory, each linking to its parent and subdirectories. Start at the root
map below and follow the per-directory files (`src/AGENTS.md`, `tools/AGENTS.md`, …)
into whichever area you're working in.

@AGENTS.md
